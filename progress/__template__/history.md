# History entry — template

> **Template.** One entry per closed feature, **appended** to
> `progress/history.md` by the `implementer` after an `APPROVED` review, at
> the same time it flips `state: done`. `progress/history.md` is append-only:
> never rewrite or delete a past entry.
>
> Copy everything below the line, replace the placeholders, append.

---

## `<YYYY-MM-DD>` — feature `<id>` `<feature-name>` — `done`

**Module:** `<module>`
**Spec:** `specs/<feature-name>/`
**Report:** `progress/impl_<feature-name>.md` · **Review:** `progress/review_<feature-name>.md`

**What shipped:** `<one or two sentences: the behaviour that now exists,
not the list of files>`

**Requirements:** R1–R`<n>`, all covered — see the traceability map in the
report.

**Review rounds:** `<1 | 2>` (`<CHANGES_REQUESTED once for the missing R3 test>`)

**Decisions worth remembering:**
- `<decision and why, only if it constrains future features>`

**Left behind on purpose:**
- `<known gap, follow-up feature id if one was created>`
